{
  "name": "postal",
  "version": "0.1.0",
  "type": "module",
  "description": "Postal — signed, sealed, append-only event protocol over git (CCDD hybrid contract).",
  "license": "MIT",
  "scripts": { "test": "node test/postal.test.mjs && node test/cli.test.mjs" }
}
